# SpanishFly root requirements (formulario principal/orquestador)
# Este entorno NO debe incluir dependencias pesadas de IA por modulo.
# Los modulos (Persona, Video, etc.) deben manejar su propio venv + requirements.

PySide6==6.9.0
pydantic==2.11.4
PyYAML==6.0.2
